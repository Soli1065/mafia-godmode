import 'package:flutter/material.dart';

class GmPanelScreen extends StatelessWidget {
  const GmPanelScreen({super.key});

  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('Night/Day/Vote controls + timers'));
}
